{
  "name": "velair-frontend",
  "version": "1.1.0-beta.1",
  "description": "Climate automation that adapts to your life.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "build:release": "npm run release-build",
    "dev": "vite build --watch",
    "release-build": "vite build",
    "test": "vitest run",
    "lint": "eslint src --ext .ts",
    "format": "prettier --write src"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "typescript": "^5.6.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
